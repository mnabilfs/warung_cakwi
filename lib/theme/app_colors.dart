import 'package:flutter/material.dart';

/// Material Design 3 Color System for Warung Cakwi
/// Based on gold (#D4A017) as primary color
class AppColors {
  AppColors._();

  // ═══════════════════════════════════════════════════════════
  // PRIMARY COLORS - Gold Theme
  // ═══════════════════════════════════════════════════════════
  static const Color primary = Color(0xFFD4A017);
  static const Color primaryLight = Color(0xFFF4C430);
  static const Color primaryDark = Color(0xFFB8860B);
  static const Color onPrimary = Color(0xFF1A1A1A);

  // ═══════════════════════════════════════════════════════════
  // SECONDARY COLORS
  // ═══════════════════════════════════════════════════════════
  static const Color secondary = Color(0xFFFFD700);
  static const Color onSecondary = Color(0xFF1A1A1A);

  // ═══════════════════════════════════════════════════════════
  // TERTIARY COLORS
  // ═══════════════════════════════════════════════════════════
  static const Color tertiary = Color(0xFFE6BE44);
  static const Color onTertiary = Color(0xFF1A1A1A);

  // ═══════════════════════════════════════════════════════════
  // SURFACE COLORS - Dark Mode
  // ═══════════════════════════════════════════════════════════
  static const Color surfaceDark = Color(0xFF1A1A1A);
  static const Color surfaceContainerDark = Color(0xFF2D2D2D);
  static const Color surfaceContainerHighDark = Color(0xFF3D3D3D);
  static const Color onSurfaceDark = Color(0xFFFFFFFF);
  static const Color onSurfaceVariantDark = Color(0xFFB0B0B0);

  // ═══════════════════════════════════════════════════════════
  // SURFACE COLORS - Light Mode
  // ═══════════════════════════════════════════════════════════
  static const Color surfaceLight = Color(0xFFFFFBFE);
  static const Color surfaceContainerLight = Color(0xFFF5F5F5);
  static const Color surfaceContainerHighLight = Color(0xFFEEEEEE);
  static const Color onSurfaceLight = Color(0xFF1A1A1A);
  static const Color onSurfaceVariantLight = Color(0xFF5C5C5C);

  // ═══════════════════════════════════════════════════════════
  // SEMANTIC COLORS
  // ═══════════════════════════════════════════════════════════
  static const Color error = Color(0xFFCF6679);
  static const Color onError = Color(0xFF1A1A1A);
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA726);

  // ═══════════════════════════════════════════════════════════
  // OUTLINE COLORS
  // ═══════════════════════════════════════════════════════════
  static const Color outlineDark = Color(0xFF4D4D4D);
  static const Color outlineLight = Color(0xFFBDBDBD);
}
